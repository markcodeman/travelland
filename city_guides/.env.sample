# Copy this to .env and fill in your keys (do NOT commit actual keys)
# Example development .env file for TravelLand (city-guides)

# Geoapify - used for geocoding, boundaries and POI discovery (recommended)
GEOAPIFY_API_KEY=your_geoapify_api_key_here

# Groq API key (Marco AI)
GROQ_API_KEY=your_groq_api_key_here