# Core dependencies
Flask>=2.3.0
requests>=2.31.0
python-dotenv>=1.0.0

# HTML parsing (for web scraping fallbacks)
beautifulsoup4>=4.12.0

# Optional: Google search fallback
# googlesearch-python>=1.2.3
